/**
 * Citation logic
 * See selections.js for form selections and dropdowns, etc
 */

document.getElementById('cite-id').addEventListener('click', () => {
    citation_format = document.getElementById('citation-format').value;



});

class MLA8 {
    constructor(
        authors, // [[fname, mname, lname], [fname, mname, lname]]
        title, // str
        website_title, // str
        publisher, // str
        url, // str
        date_published, // str
        date_accessed // str
    ) {
        
    }
}

class APA8 {
    constructor(
        authors, // [[fname, mname, lname], [fname, mname, lname]]
        title, // str
        website_title, // str
        publisher, // str
        url, // str
        date_published, // str
        date_accessed // str
    ) {
        
    }
}